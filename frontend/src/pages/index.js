import React from "react";
import { Layout } from "components/common";
import { Forms, Display } from "components/landing";

export default () => (
  <Layout>
    <Forms />
    <Display />
  </Layout>
);
